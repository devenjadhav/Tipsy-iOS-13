import UIKit
class Amount {
    var totalAmount : Float
    var amountMessage : String
    
    init(totalAmount : Float , amountMessage : String ) {
        self.totalAmount = totalAmount
        self.amountMessage = amountMessage
    }
    
}
